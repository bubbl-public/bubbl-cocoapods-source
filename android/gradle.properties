android.suppressUnsupportedCompileSdk=35
android.useAndroidX=true

# Maven identity. This intentionally reuses the legacy Android package
# coordinates so existing apps can move to v3 by changing only version.
GROUP=tech.bubbl.sdk
VERSION_NAME=3.0.2
