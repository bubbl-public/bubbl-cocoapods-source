# React Native Wrapper Tests

The current alpha test hook is `npm run test:rn` from the repository root. It validates that the JavaScript facade and TurboModule spec expose the required v3 surface.

Production readiness still requires real native Android/iOS TurboModule implementations plus serialization/event tests.
